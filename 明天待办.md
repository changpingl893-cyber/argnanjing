# 明天待办（2026-09-11 起）

## 一、部署（明天第一件事）
**问题**：`github.io` 在这台机器/这个网络下连不上（HTTP 000，代理 FlClash 规则没覆盖它），
所以线上地址一直打不开，之前看到的都是浏览器缓存旧页。

**今天已验证可用的地址**
- 电脑本地：`http://127.0.0.1:4330/`（用户已确认能看到新版）
- 手机同 WiFi：`http://10.165.237.252:4330/`（若打不开→管理员运行：
  `netsh advfirewall firewall add rule name="DSH-4330" dir=in action=allow protocol=TCP localport=4330`）
- 外网固定版本（实测 text/html 正常）：
  `https://rawcdn.githack.com/changpingl893-cyber/argnanjing/<commit>/index.html`

**两条长期方案（等用户选）**
- A：FlClash 里把 `*.github.io` 加进代理规则 → 原地址恢复（最省事）
- B：换托管 Netlify / Cloudflare Pages（我帮配，地址稳定，国内直连可用）

## 二、今天完成的内容（线索板版）
- **开场（index.html）**：黑场 → 一行行字浮现 → 停 5 秒 → **突然全黑** → 猛然弹出
  **Windows XP 弹窗**（蓝渐变标题栏 + 红 X + 米黄内容区 + 白气泡 i 图标 + 虚线按钮）；
  点【否】留下"那就先不看了"，点【是】进线索板。文件：`css/xp.css`、`js/xp-dialog.js`、`js/opening.js`
- **线索板（archive.html）**：图片全部换成 `njmuseum/photos/raw` 的 22 张原图（压缩到 `assets/relics/`）
  - 9 张**主线索卡**（点开看大图 / **双击翻面**看手写体档案，可 ← → 换张、Esc 关闭）
  - 13 张**背景卡**（互相叠压、无文字、不可点）
  - 主卡之间红线相连；板上有水印、粉笔字
  - 数据：`js/archive-data.js`（改文案/位置/归属都在这里）
- **手写体**：马善政毛笔楷书子集 `assets/fonts/ShouXie.woff2`（329KB）+ 志莽行书 `XingShu.woff2`
  - 改文案后重新子集化：`node _subset-font.js`（脚本已入库，依赖 python fontTools）

## 三、还没做的 / 可能要调
- [ ] 线索板上主卡与背景卡的**位置/重叠程度**（用户可指定哪几张靠近成组）
- [ ] 背景卡明暗（现在压得较暗）
- [ ] 开场文字浮现节奏（现在到弹窗约 16 秒，嫌慢可压缩；`data-delay` 在 index.html 里逐行可调）
- [ ] XP 弹窗尺寸/字号（现在标题 22px、正文 18px）
- [ ] 手机端实测：线索板手机布局（背景卡自动隐藏、主卡单列）、翻面手势
- [ ] 素材图（`sucai/` 里的网页/古风/配色）已按用户要求**不上线**，仅保留在本地备查
- [ ] 文案若再改，记得重跑字体子集化

## 四、命令备忘
```
# 启动本地预览（手机同 WiFi 可访问）
node _preview-server.js

# 压缩 njmuseum 原图 → assets/relics/
node _build-relics.js

# 重新生成手写体子集
node _subset-font.js
```
